[pytest]
testpaths = tests
markers =
    integration: hits the real Yahoo Finance API (deselect with -m "not integration")
addopts = --cov=main --cov-report=term-missing --cov-report=html:coverage --cov-fail-under=50
